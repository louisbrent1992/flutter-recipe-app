# Cloud Build configuration for staging environment
# This file is used to deploy the staging server to Cloud Run
#
# To set up automatic deployments:
# 1. Go to Google Cloud Console > Cloud Build > Triggers
# 2. Create a new trigger with:
#    - Name: deploy-staging
#    - Event: Push to a branch
#    - Source: Your repository
#    - Branch: ^staging$
#    - Build configuration: /server/cloudbuild-staging.yaml
#
# To deploy manually:
# gcloud builds submit --config=server/cloudbuild-staging.yaml server/

steps:
  # Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'us-west2-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/recipease-server-staging:$COMMIT_SHA'
      - '-t'
      - 'us-west2-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/recipease-server-staging:latest'
      - '.'
    dir: 'server'

  # Push the Docker image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'us-west2-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/recipease-server-staging:$COMMIT_SHA'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'us-west2-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/recipease-server-staging:latest'

  # Deploy to Cloud Run staging service
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'recipease-server-staging'  # Staging service name
      - '--image'
      - 'us-west2-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/recipease-server-staging:$COMMIT_SHA'
      - '--region'
      - 'us-west2'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--set-env-vars'
      - 'NODE_ENV=staging'
      - '--memory'
      - '512Mi'
      - '--cpu'
      - '1'
      - '--min-instances'
      - '0'
      - '--max-instances'
      - '2'  # Lower max instances for staging to save costs

# Store images in Artifact Registry
images:
  - 'us-west2-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/recipease-server-staging:$COMMIT_SHA'
  - 'us-west2-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/recipease-server-staging:latest'

# Build options
options:
  logging: CLOUD_LOGGING_ONLY

# Timeout for the entire build
timeout: '1200s'

